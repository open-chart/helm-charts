apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "operately.fullname" . }}-test-connection"
  labels:
    {{- include "operately.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "operately.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
