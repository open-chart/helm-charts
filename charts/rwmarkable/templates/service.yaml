apiVersion: v1
kind: Service
metadata:
  name: {{ include "rwmarkable.fullname" . }}
  labels:
    {{- include "rwmarkable.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "rwmarkable.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP

