apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "donetick.fullname" . }}-test-connection"
  labels:
    {{- include "donetick.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "donetick.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
